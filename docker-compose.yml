services:
  api:
    build: .
    ports:
      - "${SERVER_PORT:-8080}:8080"
    env_file:
      - .env
    environment:
      DB_HOST: host.docker.internal
    volumes:
      - ./arquivos:/app/arquivos
